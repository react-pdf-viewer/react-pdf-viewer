{
    "name": "react-pdf-viewer-parcel-demo",
    "version": "1.2.1",
    "scripts": {
        "copy": "rm -rf dist && mkdir dist && cp ../../assets/pdf-open-parameters.pdf dist",
        "dev": "npm run copy && parcel src/index.html --out-dir dist --port 8001",
        "build": "npm run copy && parcel build src/index.html --out-dir dist"
    },
    "dependencies": {
        "@phuocng/react-pdf-viewer": "^1.2.1",
        "pdfjs-dist": "^2.2.228",
        "react": "^16.12.0",
        "react-dom": "^16.12.0"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.4"
    }
}
